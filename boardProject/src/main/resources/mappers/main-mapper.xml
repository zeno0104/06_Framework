<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper
	namespace="edu.kh.project.main.model.mapper.MainMapper">

	<select id="getAllUser">
		SELECT * FROM MEMBER
		ORDER BY MEMBER_NO
	</select>
	<select id="getUser">
		SELECT * FROM MEMBER
		WHERE MEMBER_NO = #{memberNo}
	</select>

	<update id="resetPw">
		UPDATE MEMBER
		SET MEMBER_PW = #{encPw}
		WHERE MEMBER_NO
		= #{memberNo}
	</update>
	<update id="restorationMember">
		UPDATE MEMBER
		SET MEMBER_DEL_FL = 'N'
		WHERE MEMBER_NO =
		#{memberNo}
	</update>


</mapper>
